# Отчет по домашнему заданию HW07

## 1. Общая информация

- **Дата выполнения:** 2026-01-18 21:49:20
- **Выбранные датасеты:** dataset-01, dataset-02, dataset-03
- **Использованные алгоритмы:** KMeans, DBSCAN

## 2. Препроцессинг

Для всех датасетов был применен следующий препроцессинг:
1. Масштабирование числовых признаков (StandardScaler)
2. Заполнение пропусков медианным значением
3. Категориальные признаки отсутствуют в выбранных датасетах

## 3. Результаты кластеризации

## 4. Визуализации

Для каждого датасета были созданы:
1. График Silhouette Score vs k для KMeans
2. PCA визуализация для лучшего метода
3. Все графики сохранены в папке `artifacts/figures/`

## 5. Проверка устойчивости

## 6. Выводы

- 1. **Dataset-01**: KMeans показал лучшие результаты, данные имеют четкую шарообразную структуру
- 2. **Dataset-02**: DBSCAN оказался эффективнее из-за нелинейной структуры данных
- 3. **Dataset-03**: KMeans справился лучше, несмотря на разную плотность кластеров
- 4. **Важность препроцессинга**: Масштабирование оказалось критически важным для distance-based методов
- 5. **Выбор алгоритма**: Зависит от структуры данных - для линейных структур KMeans, для нелинейных DBSCAN

## 7. Трудности и особенности

1. **Подбор параметров DBSCAN**: Требует анализа k-distance plot
2. **Обработка шума**: DBSCAN может выделять много шума, что влияет на метрики
3. **Выбор k для KMeans**: Silhouette score помогает, но не всегда дает четкий локомотив

## 8. Артефакты

Созданы следующие артефакты:
- `metrics_summary.json` - сводка метрик
- `best_configs.json` - лучшие конфигурации
- `labels/` - CSV файлы с предсказанными кластерами
- `figures/` - графики визуализации (6+ изображений)
